<div class='container'>
  <div class='row'>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Donner son avis", class: "btn btn-primary" %>
<% end %>
  </div>
</div>
